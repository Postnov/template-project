include vars.pug
include mixins.pug

p Эти пункты меню выведены из переменной в #[code src/pug/partails/vars.pug]

header.header
  .header__logo-line
  nav.header__nav.nav
    ul.nav__list.container
      each item in navList
        li.nav__item
          a.nav__link(href=item.link)=item.name